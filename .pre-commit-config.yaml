repos:
  - repo: local
    hooks:
      - id: copy-codex-sessions
        name: Copy Codex Sessions
        entry: bash scripts/copy_codex_sessions.sh --dest-dir=codex_messages
        language: system
        pass_filenames: false
